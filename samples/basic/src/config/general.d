

module config.general;

enum maxTimeout = 60;

// Paginator configs

enum pageSizeDefault = 50;

// Session configs.

enum sessionDir = "/tmp/";
enum sessionTimeLimit = 20*600000000; // 20 minutes;

// Directories

enum thirdpartyDir = "/thirdparty";
enum jquerySrc = "http://code.jquery.com/jquery-1.10.2.min.js";
enum iconFileDir = "/images/icons/";
